# Example Kalshi credentials (never commit .env itself; copy and edit before running).
KALSHI_API_ID=
# Either set the key/secret directly…
KALSHI_API_KEY=
KALSHI_API_SECRET=
# …or point at a file that contains the secret text (e.g., gmac-kalshi.txt).
KALSHI_API_SECRET_FILE=
